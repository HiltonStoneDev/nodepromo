run the hostname.sh script as root

sudo ./hostname.sh

hostname.sh will:
- prompt for a new hostname
- update /etc/hostname and /etc/hosts
- set the hostname immediately
- add the nodepromo directory to git config
- pull the latest changes from git
- remove the Chromium singleton files
- reboot the system
